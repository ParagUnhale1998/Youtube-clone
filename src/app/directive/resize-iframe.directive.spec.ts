import { ResizeIframeDirective } from './resize-iframe.directive';

describe('ResizeIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeIframeDirective();
    expect(directive).toBeTruthy();
  });
});
